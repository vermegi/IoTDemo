// Write your Javascript code.

var app = angular.module('IotDemo', ['ui.bootstrap']);

app.run(function () { });

app.controller('DeviceEmulatorController', ['$rootScope', '$scope', '$http', '$timeout', function ($rootScope, $scope, $http, $timeout) {

    $scope.refresh = function () {
        
    };

}]);
